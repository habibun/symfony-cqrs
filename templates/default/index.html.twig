<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <h1>E-shop</h1>

            Search for products, order product or sign up to our SMS newsletter:

            <ul>
                <li><a href="{{ path('search') }}">Search for products</a> </li>
                <li><a href="{{ path('signup_sms') }}">Signup to SMS newsletter</a> </li>
            <li><a href="{{ path('order') }}">Finalize your e-basket</a> </li>
        </ul>
        {% endblock %}
    </body>
</html>
